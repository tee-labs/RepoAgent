# This file is auto-generated from pyproject.toml by PDM.
# To regenerate the full lock file with hashes, run: pdm lock
#
# NOTE: The previous content was a PDM lock file with pinned hashes for all
# transitive dependencies (800+ lines). It has been reset to reflect the
# dependency changes (jedi removed, codebase-memory-mcp added as core dep).
# Run `pdm lock` to regenerate the full lock file.

# Core dependencies (from [project] dependencies)
loguru>=0.7.2
GitPython>=3.1.41
prettytable>=3.9.0
python-iso639>=2024.10.22
pydantic-settings>=2.2.1
click>=8.1.7
colorama>=0.4.6
llama-index-llms-openai-like>=0.3.3
codebase-memory-mcp>=0.8.1

# Dev dependencies (from [tool.pdm.dev-dependencies])
# Install with: pdm install -G test
ruff>=0.7.4
pytest>=7.4.4,<8.0.0
pytest-mock>=3.12.0,<4.0.0
